# Configuracion de LaLupa
# Copia este archivo a /var/www/lalupa/.env y completa los valores

# Clave secreta para Flask (genera una con: python3 -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=cambia-esto-por-una-clave-secreta-larga

# URL de conexion a PostgreSQL
# Formato: postgresql://usuario:password@host:puerto/basedatos?sslmode=require
DATABASE_URL=postgresql://lalupa:TU_PASSWORD@db-postgresql-nyc1-16758-do-user-15464590-0.k.db.ondigitalocean.com:25060/defaultdb?sslmode=require

# Modo de Flask (production para produccion)
FLASK_ENV=production
